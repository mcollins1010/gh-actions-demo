name: First Workflow
on: workflow_dispatch
jobs:
  first-job:
    runs-on: macos-latest
    steps:
      - name: Print reading
        run: echo "Hello world"
      - name: Print goodbye
        run: echo "Goodbye"
